export const detectConflicts = (showOrder, dancers) => {
  // Analyze show order and return flagged dancers
  // Placeholder logic for detecting conflicts
  return dancers.filter(dancer => {
    // Example: flag if dancer has less than 2 pieces between appearances
    // This is a simplified placeholder
    return false; // No conflicts for now
  });
};